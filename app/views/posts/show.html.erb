<section id="posts" class="container inner overflow main">

	<p id="notice"><%= notice %></p>

 <article class="post">
    <div class="line"></div>
    <header>
      <h3><%= @post.title %></h3>
      <p class="price">&pound;<%= @post.price %></p>
    </header>
    <small><%= @post.posttype.name %> | <a href=""><%= @post.category.name %></a> | Negotiable Price</small>
    <p><%= @post.description %></p>
    <p><%= @post.description %></p>
    <p><%= @post.description %></p>
  </article>
</section>