<%= form_for(@posttype) do |f| %>
  <% if @posttype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@posttype.errors.count, "error") %> prohibited this posttype from being saved:</h2>

      <ul>
      <% @posttype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
